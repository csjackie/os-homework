#include <iostream>
#include <unistd.h>

int main() {
	// takes in a single command line (t)
	
	// t = the number of iterations in the for loop
	// for (i = 1; i <= t; i++) {
		//std::cout << "USER PID: " << "PPID: " << "Iteration: " << "before sleeping\n";
		//sleep(1)
		//std::cout << "USER PID: " << "PPID: " << "Iteration: " << "after sleeping\n";
	}
	return 0;
}

